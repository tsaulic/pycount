#!/usr/bin/env python
#-*- coding: utf-8 -*-


from pycount.pycount import Counter

import sys


if __name__ == "__main__":
    if len(sys.argv) is 2:
        COUNTER = Counter(sys.argv[1])
    else:
        COUNTER = Counter()
    runtime1 = COUNTER.discover()
    runtime2 = COUNTER.count()
    runtime3 = COUNTER.report()
